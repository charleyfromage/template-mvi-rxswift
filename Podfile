# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'template-mvi-rxswift' do
    # Comment the next line if you don't want to use dynamic frameworks
    use_frameworks!

    pod 'RxSwift', '6.0.0'
    pod 'RxCocoa', '6.0.0'
    pod 'RxFlow'

  # Pods for template-mvi-rxswift

    target 'template-mvi-rxswiftTests' do
        inherit! :search_paths
        # Pods for testing
    end

    target 'template-mvi-rxswiftUITests' do
        # Pods for testing
    end

end
